spring:
  application:
    name: lms-entrepreneurship-school-backend-app
  database:
    master:
      url: r2dbc:postgresql://lms_dev_backend:lms_backend_dev_password@localhost:5433/lms_db
    slave:
      url: r2dbc:postgresql://lms_dev_backend:lms_backend_dev_password@localhost:5434/lms_db

  docker:
    compose:
      enabled: false

logging:
  level:
    org.springframework.r2dbc: TRACE
    ru.hse.lmsteam: DEBUG

server:
  port: 3000
  compression:
    enabled: true

app:
  proto:
    schema:
      path: ru.hse.lmsteam.schema

springdoc:
  show-actuator: true

application:
  security:
    jwt:
#      need to be at least 512 bits long (86+ characters)
      secret-key: Gj423j1LGJghsldjasjfsdLGJASLFldfiw1x94092swsmchsfasg202sdfsf42ljl2j430sdfsfsdf080d8flj23jl242sdlfj239anv9w8l23slom9rhnhf3sjdiwe80384hihrhh238asdhkfhskfhsk2323fkhskkamnwqvg37217yehdjghf
      expiration: 86400 # 1 day